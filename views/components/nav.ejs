<nav class="navbar">
    <a class="navbar-brand" href="/">LSE Cinema</a>
    <input type="checkbox" id="toggler">
    <%if((pageTitle=="Home"||pageTitle=="order") && token){%>
        <% if(token){%>
            <div class="user">
                <p>Hi <%= user%></p>
                <a href="/logout">Log out</a>
            </div>
        <%}%>
    <%}%>
    <label for="toggler"><i class="ri-menu-line"></i></label>
    <div class="menu">
        <ul class="list">
            <li><a href="/"><%= nav1 %></a></li>
            <%if(pageTitle!="Home" || !token){%>
                <li><a href="<%= link %>"><%= nav2 %></a></li>
            <%}%>
        </ul>
    </div>
</nav>